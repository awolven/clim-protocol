(in-package :cl-user)

(defclass clim:medium () ())

(defclass clim:port () ())

(defclass clim:mirrored-sheet-mixin () ())

(defclass clim:presentation () ())

(defvar clim:*default-frame-manager*)
